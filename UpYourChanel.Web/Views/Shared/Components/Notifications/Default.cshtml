@using UpYourChannel.Web.ViewModels.Userr
@model UserViewModel
@if (Model.NotificationsCount.ToString() != "0")
{
    <span class="notify-badge">@Model.NotificationsCount.ToString()</span>
}
<img src=@Model.ProfilePictureUrl class="d-block rounded-circle" style="border-radius:50%; width:50px; height:50px" alt="" />
