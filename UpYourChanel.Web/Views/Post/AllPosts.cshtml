@model UpYourChannel.Web.ViewModels.Post.AllPostsViewModel

<div class="text-center">
    <br />
    <h1 class="display-4">Number 1 Youtube forum!</h1>
</div>
<br />
<br />

@foreach (var post in Model.Posts)
{
    <div class="media-body">
        <h4 class="media-heading">@post.Title</h4>
        <p>@post.Content</p>
        <ul class="list-inline list-unstyled text-left">
            <li class="list-inline-item">
                <i class="fas fa-user-edit"></i> @post.UserUserName
            </li>
            <li class="list-inline-item">
                <i class="far fa-calendar"></i> @post.CreatedOn
            </li>
            <li class="list-inline-item">
                <i class="fas fa-comments"></i> @post.CommentsCount
            </li>
        </ul>
    </div>
}